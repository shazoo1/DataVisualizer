<Window x:Class="DataVisualizer.Desktop.Views.PlotTypeSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataVisualizer.Desktop.Views"
        mc:Ignorable="d"
        Title="Diagramm Typ auswählen" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition MaxHeight="50"/>
        </Grid.RowDefinitions>
        
            <ListBox Grid.ColumnSpan="2"
                ItemsSource="{Binding GraphTypes}"
                SelectedItem="{Binding SelectedItem}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Label}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Command="{Binding CancelCommand}" Grid.Column="0" HorizontalAlignment="Stretch">Abbrechen</Button>
            <Button Command="{Binding OkCommand}" Grid.Column="1" HorizontalAlignment="Stretch">Ok</Button>
        </Grid>
    </Grid>
</Window>
